{
  "name": "solar-dashboard",
  "version": "0.8.1",
  "description": "Solar monitoring dashboard for Home Assistant",
  "type": "module",
  "scripts": {
    "build": "rollup -c",
    "watch": "rollup -c --watch"
  },
  "devDependencies": {
    "rollup": "^4.0.0",
    "@rollup/plugin-node-resolve": "^15.0.0",
    "@rollup/plugin-terser": "^0.4.0"
  }
}
